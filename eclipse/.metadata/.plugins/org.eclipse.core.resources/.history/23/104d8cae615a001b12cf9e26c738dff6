package ru.GraindGreed.MMod;

import cpw.mods.fml.common.registry.GameRegistry;
import cpw.mods.fml.relauncher.Side;
import cpw.mods.fml.relauncher.SideOnly;
import net.minecraft.block.Block;
import net.minecraft.block.material.Material;
import net.minecraft.client.Minecraft;
import net.minecraft.entity.player.EntityPlayer;
import net.minecraft.server.MinecraftServer;
import net.minecraft.util.AxisAlignedBB;
import net.minecraft.world.World;

public class BlockTinOre extends Block {

	public BlockTinOre(Material rock, String name, String texture) {
		super(Material.rock);
		this.setBlockName(name);
		this.setLightLevel(0F);
		this.setLightOpacity(10);
		this.setHardness(0.2F);
		this.setCreativeTab(Main.tabMModIndustry);
		this.setResistance(10);
		this.setHarvestLevel("pickaxe", 1);
		this.setStepSound(soundTypeStone);
		this.setBlockTextureName(Main.MODID + ":" + texture);
		GameRegistry.registerBlock(this, name);
	}
	
//	public AxisAlignedBB getCollisionBoundingBoxFromPool(World world, int x, int y, int z) {
//		return null;
//	}
	
//	@SideOnly(Side.CLIENT)
//	public boolean onBlockActivated(World world, int p2, int p3, int p4, EntityPlayer player, int p6, float p7, float p8, float p9) {
//		Minecraft.getMinecraft().thePlayer.sendChatMessage("Да ты заибал!");
//		MinecraftServer.getServer().getCommandManager().executeCommand(MinecraftServer.getServer(), "weather thunder");
//		return true;
//	}

}
